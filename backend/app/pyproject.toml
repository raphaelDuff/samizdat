[project]
name = "samizdat"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "sqlmodel>=0.0.31",
]

[dependency-groups]
dev = [
    "ruff>=0.14.8",
]


[tool.ruff.lint]
extend-select = ["I"]
